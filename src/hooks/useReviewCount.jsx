


const useReviewCount = () => {
    // const [reviewCount, setReviewCount] = useState(0);
    // const [reviewRating,setReviewRating] = useState(0);
    // const rooms = useRooms();
    // console.log(rooms);
    // //setReviewCount(rooms.reviews.length);
    
	// // average rating from reviews
	// const totalReviewRating = rooms.reviews.reduce(
	// 	(total, review) => total + review.rating,
	// 	0
	// );
	// const avgRating = totalReviewRating / rooms.reviews.length;
	// console.log(avgRating);
    // setReviewRating(avgRating);
    // console.log(reviewCount,reviewRating);
   
    // return reviewCount,reviewRating;
};

export default useReviewCount;